URGENT - Le serveur prospup.work n'a pas récupéré les changements. La pastille de version ne fonctionne pas car le serveur retourne "Non authentifié" au lieu de la version.

Sur le PC qui héberge prospup.work, dans le dossier du projet, exécute ces commandes dans l'ordre :

1. cd "CHEMIN_VERS_PROSPUP"  (remplacer par le vrai chemin)
2. git checkout main
3. git pull origin main
4. Vérifier que ça a marché : git log --oneline -1 (doit afficher e5c6324 ou plus récent)

Si le superviseur tourne déjà, il va redémarrer automatiquement. Sinon, le lancer avec : python scripts/supervise_prospup.py

Pour diagnostiquer pourquoi le superviseur n'a pas fait le pull automatiquement, exécuter : python scripts/diagnostic-superviseur.bat (ou .sh sur Linux)

C'est tout. Exécute ces commandes maintenant.

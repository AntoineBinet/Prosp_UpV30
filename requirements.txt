# PyTorch CUDA index — installé en plus du PyPI pour récupérer les wheels GPU.
# Sans GPU : commenter cette ligne, pip prendra alors les wheels CPU depuis PyPI.
--extra-index-url https://download.pytorch.org/whl/cu121

flask>=2.2
openpyxl>=3.1
waitress>=2.1
rjsmin>=1.2
csscompressor>=0.9
pypdf>=4.0
python-docx>=1.0
reportlab>=4.0
apscheduler>=3.10
pymupdf>=1.24
Pillow>=10.0

# v32.1 — Transcription de réunions (Whisper + pyannote + Claude)
# Téléchargement initial ~3 GB (torch CUDA + modèles Whisper/pyannote
# en lazy load à la première transcription). Le timeout d'install est
# bumpé à 1200 s côté deploy (routes/deploy.py).
torch>=2.1
torchaudio>=2.1
faster-whisper>=1.0
pyannote.audio>=3.1
